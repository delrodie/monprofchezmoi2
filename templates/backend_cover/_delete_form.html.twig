<form method="post" action="{{ path('backend_cover_delete', {'id': cover.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette cover?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cover.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
