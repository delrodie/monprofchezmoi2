<form method="post" action="{{ path('backend_media_delete', {'id': medium.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce media?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medium.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
