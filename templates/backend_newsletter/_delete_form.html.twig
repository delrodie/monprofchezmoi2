<form method="post" action="{{ path('app_backend_newsletter_delete', {'id': newsletter.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce compte newsletter?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newsletter.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
