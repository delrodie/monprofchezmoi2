{% extends 'base.html.twig' %}

{% block title %}MenuAdulte{% endblock %}

{% block body %}
    <h1>MenuAdulte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu_adulte.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ menu_adulte.titre }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ menu_adulte.slug }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ menu_adulte.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_menu_adulte_index') }}">back to list</a>

    <a href="{{ path('backend_menu_adulte_edit', {'id': menu_adulte.id}) }}">edit</a>

    {{ include('backend_menu_adulte/_delete_form.html.twig') }}
{% endblock %}
