<form method="post" action="{{ path('backend_menu_adulte_delete', {'id': menu_adulte.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce menu?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu_adulte.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
