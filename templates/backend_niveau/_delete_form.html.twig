<form method="post" action="{{ path('backend_niveau_delete', {'id': niveau.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce niveau?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ niveau.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
