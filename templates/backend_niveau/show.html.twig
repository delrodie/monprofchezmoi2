{% extends 'base.html.twig' %}

{% block title %}Niveau{% endblock %}

{% block body %}
    <h1>Niveau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ niveau.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ niveau.type }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ niveau.libelle }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ niveau.statut ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ niveau.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_niveau_index') }}">back to list</a>

    <a href="{{ path('backend_niveau_edit', {'id': niveau.id}) }}">edit</a>

    {{ include('backend_niveau/_delete_form.html.twig') }}
{% endblock %}
