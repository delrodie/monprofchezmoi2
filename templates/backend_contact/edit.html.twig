{% extends 'base.html.twig' %}

{% block title %}Edit Contact{% endblock %}

{% block body %}
    <h1>Edit Contact</h1>

    {{ include('backend_contact/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('backend_contact_index') }}">back to list</a>

    {{ include('backend_contact/_delete_form.html.twig') }}
{% endblock %}
